// Generated by CoffeeScript 1.12.7
(function() {
  var appRoot, clearList, count, deside, hasOptions, pageData;

  pageData = {
    title: 'Indesision App',
    subTitle: 'Helping you make desisions',
    options: []
  };

  hasOptions = function(options) {
    if (options.length > 0) {
      return '<p>Here are your options</p>';
    }
  };

  count = 0;

  $(document).ready(function() {
    return $('form').submit(function(e) {
      alert(e.target.elements.test.value);
    });
  });

  clearList = function() {
    pageData.options = [];
    return renderPage();
  };

  deside = function() {
    var option, randomNum;
    randomNum = Math.floor(Math.random() * pageData.options.length);
    option = pageData.options[randomNum];
    return alert(option);
  };

  appRoot = document.getElementById('app');

}).call(this);
